import { VocalsToNumberPipe } from './vocals-to-number.pipe';

describe('VocalsToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VocalsToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
